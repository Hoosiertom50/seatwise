node_modules/
.next/
.turbo/
dist/
.env
.env*.local
*.log
.DS_Store
.pnpm-store/
generated/

# Playwright Quality Engineering Framework: generated run output, reports, and diagnostic
# artifacts are never committed -- only the framework source/templates that produce them are.
# The directory skeleton itself (via .gitkeep) stays tracked so it exists on a clean checkout.
artifacts/playwright/**
!artifacts/playwright/**/
!artifacts/playwright/**/.gitkeep
# Local database dumps (packages/db/prisma/backup-real-weddings.ts). These contain real wedding
# and guest rows -- names, party details, and free-text notes -- so they are never committed.
artifacts/db-backups/

/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
